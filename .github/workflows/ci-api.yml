name: 'CI: api'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'
    paths:
      - packages/@ril/api-client/**
      - packages/@ril/api-spec/**
      - packages/@ril/api/**
      - packages/@ril/eslint-plugin/**
      - packages/@ril/tsconfig/**

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      TZ: 'Asia/Tokyo'
      DATABASE_URL: mysql://user:password@localhost:3306/test
      YOUTUBE_DATA_API_KEY: ${{ secrets.YOUTUBE_DATA_API_KEY }}
      YOUTUBE_WEBSUB_HMAC_SECRET: test

    services:
      db:
        image: mysql:8.0
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: test
          MYSQL_USER: user
          MYSQL_PASSWORD: password
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Yarn
        run: |
          yarn workspaces focus @ril/api

      - name: Build
        run: |
          yarn workspaces foreach -pR --topological-dev --from @ril/api run build

      - name: Tests
        run: |
          yarn workspace @ril/api run test

      - name: Codecov
        uses: codecov/codecov-action@v1.0.15
        with:
          flags: api
          token: ${{ secrets.CODECOV_TOKEN }}
