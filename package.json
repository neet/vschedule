{
  "name": "ril",
  "private": true,
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "build:api": "yarn workspace @ril/api-spec run build && yarn workspace @ril/api-client run build"
  },
  "workspaces": [
    "packages/@ril/*"
  ],
  "devDependencies": {
    "@types/prettier": "^2",
    "prettier": "^2.7.1"
  }
}
