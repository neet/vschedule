{
  "name": "vschedule",
  "private": true,
  "packageManager": "yarn@3.2.4",
  "scripts": {
    "build:api": "yarn workspace @neet/vschedule-api-spec run build && yarn workspace @neet/vschedule-client run codegen && @neet/vschedule-api run codegen"
  },
  "workspaces": [
    "packages/@neet/*"
  ],
  "devDependencies": {
    "@types/prettier": "^2",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "@lerna-lite/cli": "^1.12.0"
  }
}
